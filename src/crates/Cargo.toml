[workspace]
members = [
  "sonar-audio",
  "sonar-core",
  "sonar-dictation",
  "sonar-models",
  "sonar-transcription",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
lto = true
strip = "symbols"
